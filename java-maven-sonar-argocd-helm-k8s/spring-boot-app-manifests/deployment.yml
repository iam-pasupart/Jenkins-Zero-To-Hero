apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-app
  namespace: three-tier
  labels:
    app: spring-boot-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-boot-app
  template:
    metadata:
      labels:
        app: spring-boot-app
    spec:
      containers:
      - name: spring-boot-app
        image: mochap/ultimate-cicd:17
        ports:
        - containerPort: 3000
